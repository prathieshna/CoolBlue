pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        //noinspection JcenterRepositoryObsolete
        jcenter() // For ReKotlin Lib TODO Read-Only. Remove once Lib is available in mavenCentral
    }
}
rootProject.name = "Coolblue"
include ':app'
